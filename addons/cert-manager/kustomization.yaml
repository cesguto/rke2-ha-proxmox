# cert-manager: manifesto oficial (release fixo). Aplicar com: kubectl apply -k .
# Requer acesso de rede do cluster/API ao download do release no primeiro apply.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://github.com/cert-manager/cert-manager/releases/download/v1.16.2/cert-manager.yaml
