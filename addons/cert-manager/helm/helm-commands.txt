# Equivalente a esta pasta, usando Helm diretamente (repositórios: ver ../../../repositorios-helm.txt)

helm repo add jetstack https://charts.jetstack.io
helm repo update

helm upgrade --install cert-manager jetstack/cert-manager \
  --namespace cert-manager \
  --create-namespace \
  --version v1.16.2 \
  -f values.yaml
