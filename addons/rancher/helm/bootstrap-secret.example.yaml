# Cópia: renomeie ou copie ajustando a password e:
#   kubectl apply -f bootstrap-secret.yaml
# (Não comite a password em claro; prefira o comando create abaixo em CI/secret manager.)
apiVersion: v1
kind: Secret
metadata:
  name: bootstrap-secret
  namespace: cattle-system
type: Opaque
stringData:
  bootstrapPassword: "linux2222"
