version: 2
ethernets:
  enp6s18:               # Replace with your VM NIC name if different
    dhcp4: false
    addresses:
      - ${ip}/${cidr_prefix}
    routes:
        - to: 0.0.0.0/0
          via: 192.168.0.1
    #gateway4: ${gateway}
    nameservers:
      addresses:
${dns_addresses_yaml}